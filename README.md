# Junit-Automated-Simple-Getter-Setter-Testing
Create automated unit testing for simple getters / setters within a package and its subpackages using a class of the root package as the starting point.
